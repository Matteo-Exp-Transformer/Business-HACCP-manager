import type { AppState, Entities } from "../types/entities";

export function deriveEntitiesFromOnboarding(onb: AppState["onboarding"]): Partial<Entities> {
  // TODO: implementa mapping reale replicando lo stile della Tab 2
  return {};
}